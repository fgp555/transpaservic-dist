import { OrderEntity, OrderStatus } from '../order/entities/order.entity';
import { Repository } from 'typeorm';
import { WaContactsService } from './contacts.service';
export declare class WaSenderService {
    private readonly orderRepository;
    private readonly contactsService;
    private apiBaseUrl;
    private token;
    private phoneNumberId;
    private apiUrl;
    constructor(orderRepository: Repository<OrderEntity>, contactsService: WaContactsService);
    processReplyMessage(message: any, contactName: string): Promise<void>;
    private sendSupportTemplate;
    sendTextMessage(to: string, body: string): Promise<any>;
    private sendInteractiveMessage;
    private templateOrders;
    findOrders(userPhone: string): Promise<{
        operator: string;
        id: number;
        orderNumber: string;
        patientName: string;
        documentType: import("../order/entities/order.entity").DocumentType;
        idCard: string;
        userPhone: string;
        itinerary: string;
        creationDate: Date;
        expirationDate: Date | null;
        travelDate: Date | null;
        approvalDate: Date | null;
        approvalTravelDate: Date | null;
        ticketNumber: string;
        quantity: number;
        approvalQuantity: number;
        authorizationNumber: string;
        operatorContract: string;
        value: number;
        netValue: number;
        origin: string;
        destination: string;
        client: string;
        remarks: string;
        status: OrderStatus;
        ticketImage: string;
        dimensionImg: string;
        backticketHistory: import("../order/entities/back-ticket.entity").BackTicketEntity[];
        email: string;
        orderHistory: import("../order/entities/order-history.entity").OrderHistoryEntity[];
        createdAt: Date;
        updatedAt: Date;
    }[]>;
}
